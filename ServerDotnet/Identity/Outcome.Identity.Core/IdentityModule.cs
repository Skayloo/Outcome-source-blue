namespace Outcome.Identity;

/// <summary>Assembly marker for the Identity module (auth, users, roles, invites, server admin).
/// Used to register the module's MediatR handlers + validators.</summary>
public static class IdentityModule { }
