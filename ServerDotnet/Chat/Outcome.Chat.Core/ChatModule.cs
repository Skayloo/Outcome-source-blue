namespace Outcome.Chat;

/// <summary>Assembly marker for the Chat module (channels, messages, DMs, voice, search, media).
/// Used to register the module's MediatR handlers + validators.</summary>
public static class ChatModule { }
